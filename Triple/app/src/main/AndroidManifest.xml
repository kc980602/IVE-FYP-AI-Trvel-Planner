<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.triple.triple">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppThemeNoActionBar">
        <activity
            android:name=".View.Home.HomeActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.Mytrips.MytripsActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".View.Search.SearchActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".View.Profile.ProfileActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".View.Mytrips.TripCreateActivity"
            android:parentActivityName=".View.Mytrips.MytripsActivity"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="stateVisible" />
        <activity
            android:name=".View.Mytrips.TripDetailActivity"
            android:parentActivityName=".View.Mytrips.MytripsActivity"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>